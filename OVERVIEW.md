# Обзор проекта: P2P-платформа для аренды автомобилей

## 📋 Описание проекта

Данное веб-приложение представляет собой P2P-платформу для аренды автомобилей между частными лицами. Система разработана как Single Page Application (SPA) без использования `react-router`, что обеспечивает быструю навигацию между разделами без перезагрузки страницы.

**Основная цель проекта**: Создание удобной платформы для прямой аренды автомобилей между пользователями с возможностью управления автопарком, бронированиями и отзывами.

## 🎯 Основные возможности системы

### Для всех пользователей (включая неавторизованных):

1. **Просмотр каталога автомобилей**
   - Фильтрация по типу топлива, коробке передач
   - Поиск по марке и модели
   - Отображение информации об автомобилях

2. **Просмотр аренд**
   - Просмотр списка активных аренд
   - Фильтрация по статусу

3. **Просмотр локаций**
   - Список доступных точек выдачи
   - Информация о локациях

### Для авторизованных пользователей (Владелец):

1. **Управление автомобилями**
   - Добавление своих автомобилей
   - Редактирование информации
   - Изменение статуса доступности

2. **Управление арендами**
   - Подтверждение и отклонение заявок
   - Отслеживание статуса аренд

### Для администраторов:

Все функции владельца, плюс:

1. **Управление автомобилями** (CRUD операции)
   - Добавление и редактирование любых авто
   - Деактивация автомобилей
   - Полный контроль над каталогом

2. **Управление локациями** (CRUD операции)
   - Создание и редактирование локаций
   - Деактивация локаций

3. **Управление арендами** (CRUD операции)
   - Полный доступ ко всем арендам
   - Изменение статусов

## 🛠 Технологический стек

### Frontend:

- **React 19.2.0** - библиотека для создания пользовательского интерфейса
- **TypeScript 5.9.3** - типизированный JavaScript для надежности кода
- **Vite (rolldown-vite 7.3.0)** - современный инструмент сборки
- **MobX 6.15.0** - библиотека для управления состоянием приложения
- **SCSS/SASS 1.97.1** - препроцессор CSS для стилизации
- **Firebase 12.7.0** - база данных (Firebase Realtime Database)

### Инструменты разработки:

- **ESLint** - линтер для проверки качества кода
- **TypeScript ESLint** - проверка типов TypeScript

## 💡 Почему React, а не Laravel (PHP)?

Проект реализован на React вместо традиционного стека Laravel + PHP по двум ключевым причинам:

1. **Встроенная безопасность после сборки (обфускация кода)**
   При выполнении команды `npm run build` весь исходный код проекта проходит через процесс **минификации и обфускации**. Это означает, что итоговые JavaScript-файлы становятся практически нечитаемыми для человека — переменные переименовываются в короткие бессмысленные имена, убираются все комментарии, код сжимается. В результате, даже если кто-то получит доступ к файлам на хостинге, восстановить исходную логику приложения будет крайне затруднительно. В Laravel исходный PHP-код остаётся на сервере в читаемом виде.

2. **Бесплатный хостинг на статичных платформах**
   После сборки React-приложение превращается в набор обычных статичных файлов (HTML, CSS, JS), которые можно разместить на **любом бесплатном статичном хостинге**, например:
   - **Vercel** — деплой в один клик, автоматический CI/CD
   - **GitHub Pages** — бесплатный хостинг прямо из репозитория

   Laravel и PHP требуют серверный хостинг с поддержкой PHP и базы данных, что практически всегда **платно**, либо доступно только с жёсткими ограничениями и сложной настройкой.

## 📁 Архитектура и структура проекта

```
Car-Rental-P2P_Damir/
├── src/
│   ├── components/          # Переиспользуемые UI компоненты
│   │   ├── UI/             # Базовые компоненты (Button, Input, Modal, Table и т.д.)
│   │   ├── Layout/         # Компоненты макета (Header, Sidebar, MainLayout)
│   │   ├── LoginModal/     # Модальное окно авторизации
│   │   └── ConfirmModal/   # Модальное окно подтверждения действий
│   │
│   ├── pages/              # Страницы приложения
│   │   ├── HomePage/       # Главная страница с дашбордом
│   │   ├── CarsPage/       # Страница каталога авто
│   │   ├── RentalsPage/    # Страница аренд
│   │   ├── LocationsPage/  # Страница локаций
│   │   └── AdminPage/      # Страница администрирования
│   │
│   ├── store/              # Управление состоянием (MobX stores)
│   │   ├── AuthStore.ts    # Авторизация и роли пользователей
│   │   ├── DataStore.ts    # CRUD операции с данными
│   │   ├── NavigationStore.ts # Навигация между страницами
│   │   └── UIStore.ts      # UI состояние (модалки, уведомления)
│   │
│   ├── types/              # TypeScript типы и интерфейсы
│   │   ├── user.types.ts   # Типы пользователей и ролей
│   │   ├── car.types.ts    # Типы автомобилей
│   │   ├── rental.types.ts # Типы аренд
│   │   ├── location.types.ts # Типы локаций
│   │   ├── review.types.ts # Типы отзывов
│   │   └── navigation.types.ts # Типы навигации
│   │
│   ├── styles/             # Глобальные стили
│   │   ├── globalColors.scss # Цветовая палитра (сине-оранжевая тема)
│   │   ├── variables.scss  # CSS переменные
│   │   ├── mixins.scss     # SCSS миксины
│   │   └── global.scss     # Глобальные стили
│   │
│   ├── hooks/              # Кастомные React хуки
│   ├── utils/              # Утилиты
│   ├── firebase.ts         # Конфигурация Firebase
│   └── App.tsx             # Главный компонент приложения
│
├── CREDENTIALS.md          # Учетные данные для входа
├── OVERVIEW.md             # Этот файл - обзор проекта
└── package.json            # Зависимости и скрипты
```

## 🔐 Система ролей и прав доступа

### Роли пользователей:

1. **guest** (Гость) - роль по умолчанию
   - Просмотр каталога авто, аренд, локаций
   - Нет возможности редактирования данных

2. **owner** (Владелец) - требует авторизацию
   - Все права гостя
   - Управление своими автомобилями
   - Управление своими арендами

3. **admin** (Администратор) - требует авторизацию
   - Все права владельца
   - Управление всеми автомобилями (CRUD)
   - Управление локациями (CRUD)
   - Доступ к разделу "Администрирование"

**Пароли для входа** находятся в файле `CREDENTIALS.md`

## 🗄 Структура данных

### Основные сущности:

1. **Car (Автомобиль)**
   - id, brand, model, year
   - fuelType, transmission
   - pricePerDay, status
   - ownerId, locationId, isActive

2. **Rental (Аренда)**
   - id, carId, renterId
   - startDate, endDate
   - totalPrice, status
   - notes

3. **Location (Локация)**
   - id, name, address
   - city, coordinates
   - isActive

4. **Review (Отзыв)**
   - id, carId, userId
   - rating, comment
   - date

## 🚀 Запуск проекта

### Предварительные требования:

- Node.js (рекомендуется версия 18 или выше)
- npm

### Шаги запуска:

1. **Установка зависимостей:**
   ```bash
   npm install
   ```

2. **Запуск development сервера:**
   ```bash
   npm run dev
   ```

## 📚 Что желательно понимать для защиты диплома

### Теоретическая часть:

1. **SPA (Single Page Application)**
   - Что такое SPA и его преимущества? Ответ: SPA - это веб-приложение, которое загружается один раз, а затем все переходы между страницами происходят без перезагрузки. Преимущества: быстрая работа, плавные переходы, лучший UX.
   - Как реализована навигация? Ответ: Через NavigationStore (MobX), который переключает компоненты в App.tsx.

2. **State Management (MobX)**
   - Зачем нужен MobX? Ответ: Для централизованного управления состоянием приложения.
   - Как организованы stores? Ответ: AuthStore (авторизация), DataStore (данные), NavigationStore (навигация), UIStore (UI).

3. **TypeScript**
   - Преимущества типизации? Ответ: Обнаружение ошибок на этапе компиляции.

4. **Firebase Realtime Database**
   - Что такое Firebase? Ответ: Облачная NoSQL база данных от Google.

5. **P2P модель**
   - Что такое P2P? Ответ: Peer-to-peer - модель, где пользователи взаимодействуют напрямую друг с другом.

### Возможные вопросы:

1. **"Как обеспечивается безопасность аренды?"**
   - Проверка прав на уровне ролей
   - Валидация данных

2. **"Как работает процесс аренды?"**
   - Создание заявки → Подтверждение владельцем → Аренда активна → Завершение

## 🔍 Краткий экскурс в проект для защиты

Если вы знакомы с HTML, CSS и JavaScript, но не работали с React и TypeScript — этот раздел поможет вам быстро понять, как устроен проект и что где находится.

### Что такое React?

React — это JavaScript-библиотека для создания пользовательских интерфейсов. Вместо обычного HTML здесь используется **JSX** (JavaScript XML) — расширение синтаксиса JS, которое позволяет писать разметку прямо в JavaScript-коде. Файлы с расширением `.tsx` — это то же самое, но с поддержкой TypeScript.

**Простая аналогия**: если в обычном веб-сайте у вас есть `index.html` + `script.js`, то в React весь HTML и JS объединены в **компоненты** — функции, которые возвращают разметку.

### Что такое TypeScript?

TypeScript — это JavaScript с **типизацией**. Он добавляет возможность указывать типы переменных, параметров функций и возвращаемых значений. Это помогает находить ошибки ещё до запуска кода. После сборки TypeScript превращается в обычный JavaScript.

**Простая аналогия**: TypeScript — это как проверка орфографии для кода. Файлы `types/*.types.ts` описывают «форму» данных — какие поля есть у объекта и какого они типа. Это похоже на описание колонок в таблице базы данных, но на уровне кода.

### Как устроен проект?

#### 1. Точка входа — `main.tsx` и `App.tsx`

Всё начинается с файла `main.tsx`, который рендерит (отображает) компонент `App` на странице. `App.tsx` — это главный компонент приложения. При загрузке он вызывает `dataStore.loadAllData()` — загружает все данные из Firebase, оборачивает содержимое в `MainLayout` (шапка + боковое меню + контент) и содержит `PageRouter` — компонент, который решает, какую страницу показать.

#### 2. Навигация (без react-router)

В проекте **нет** библиотеки `react-router`. Навигация реализована вручную через **MobX store**:

- `NavigationStore` хранит переменную `currentPage` (например, `'home'`, `'cars'`, `'rentals'`, `'locations'`, `'admin'`)
- Компонент `PageRouter` в `App.tsx` через `switch/case` проверяет `currentPage` и рендерит нужную страницу:
  - `'home'` → `HomePage` (дашборд)
  - `'cars'` → `CarsPage` (каталог автомобилей)
  - `'rentals'` → `RentalsPage` (аренды)
  - `'locations'` → `LocationsPage` (локации выдачи)
  - `'admin'` → `AdminPage` (администрирование)
- Когда пользователь нажимает на пункт в боковом меню (`Sidebar`), вызывается `navigationStore.navigate('cars')`, что меняет `currentPage`, и React автоматически перерисовывает нужную страницу

#### 3. Управление состоянием — MobX

**MobX** — это библиотека для управления состоянием приложения (аналог «глобальных переменных», доступных из любого компонента). В проекте 4 store:

- **AuthStore** (`store/AuthStore.ts`) — авторизация и роли пользователей
- **DataStore** (`store/DataStore.ts`) — все данные приложения (автомобили, аренды, локации, отзывы) и CRUD-операции
- **NavigationStore** (`store/NavigationStore.ts`) — текущая страница и навигация
- **UIStore** (`store/UIStore.ts`) — управление модальными окнами, уведомлениями, темой

Компоненты оборачиваются в `observer()` из `mobx-react-lite` — это позволяет React автоматически перерисовывать компонент при изменении данных в store.

#### 4. Авторизация и роли

`AuthStore` управляет системой ролей:

- **guest** (Гость) — роль по умолчанию, только просмотр каталога, аренд и локаций
- **owner** (Владелец) — управление своими автомобилями и арендами
- **admin** (Администратор) — полный доступ ко всему, включая CRUD для автомобилей и локаций
- Пароли захардкожены в коде (это учебный проект, не production)
- Сессия сохраняется в `localStorage` на 24 часа
- Проверка прав: объект `ROLE_PERMISSIONS` определяет, что может делать каждая роль
- При попытке перейти на защищённую страницу без авторизации открывается модальное окно входа

#### 5. Работа с данными — Firebase

`firebase.ts` инициализирует подключение к **Firebase Realtime Database** (облачная NoSQL база данных от Google). Класс `FirebaseService` предоставляет методы:

- `getData(path)` — получить данные
- `setData(path, data)` — записать данные
- `updateData(path, updates)` — обновить данные

`DataStore` использует эти методы для CRUD-операций с автомобилями, арендами, локациями и отзывами. Каждая сущность хранится в массиве, при создании/редактировании данные сначала отправляются в Firebase, а затем обновляются локально.

#### 6. Компоненты и страницы

**Компоненты** (`src/components/`):
- `UI/` — переиспользуемые элементы: Button, Input, Modal, Table, Select, Badge, Card
- `Layout/` — структура страницы: Header (шапка), Sidebar (боковое меню), MainLayout (общий каркас)
- `LoginModal/` — модальное окно авторизации
- `ConfirmModal/` — модальное окно подтверждения действий (например, удаления)

**Страницы** (`src/pages/`):
- `HomePage/` — дашборд со статистикой по автомобилям, арендам и локациям
- `CarsPage/` — каталог автомобилей с фильтрацией по типу топлива, коробке передач
- `RentalsPage/` — список аренд с возможностью создания заявок и изменения статуса
- `LocationsPage/` — точки выдачи автомобилей с информацией об адресах
- `AdminPage/` — панель администрирования с CRUD-операциями для всех сущностей

Каждая страница — это папка с тремя файлами:
- `[PageName].tsx` — React-компонент с логикой и разметкой
- `[PageName].module.scss` — стили (CSS-модули + SCSS)
- `index.ts` — реэкспорт компонента для удобного импорта

#### 7. Стили

Проект использует **SCSS** (расширение CSS) с модульным подходом:
- `*.module.scss` — стили, привязанные к конкретному компоненту (не конфликтуют с другими)
- `styles/globalColors.scss` — цветовая палитра проекта (сине-оранжевая тема)
- `styles/variables.scss` — CSS-переменные (размеры, отступы, шрифты)
- `styles/mixins.scss` — повторно используемые блоки стилей
- `styles/global.scss` — глобальные стили для всего приложения

---

**Удачи на защите! 🎉**
